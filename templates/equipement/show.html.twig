{% extends 'base.html.twig' %}

{% block title %}Equipement{% endblock %}
{% block body %}
<body>

    <h1>Equipement</h1>

   <table class="table table-bordered table-striped">
    <tbody>
        <tr>
            <th scope="row" style="width: 20%;">Id</th>
            <td>{{ equipement.id }}</td>
        </tr>
        <tr>
            <th scope="row">Nom</th>
            <td>{{ equipement.Nom }}</td>
        </tr>
        <tr>
            <th scope="row">Description</th>
            <td>{{ equipement.Discription }}</td>
        </tr>
        <tr>
            <th scope="row">Etat</th>
            <td>{{ equipement.etat }}</td>
        </tr>
        <tr>
            <th scope="row">Disponibilite</th>
            <td>{{ equipement.Disponibilite ? 'Yes' : 'No' }}</td>
        </tr>
    </tbody>
</table>

    <a class="btn btn-secondary" href="{{ path('app_equipement_index') }}">back to list</a>

   <a class="btn btn-primary"  class="btn btn-success" href="{{ path('app_equipement_edit', {'id': equipement.id}) }}">edit</a>
</br>
   <div> </br>
{{ include('equipement/_delete_form.html.twig') }}</div>
</body>
{% endblock %}
