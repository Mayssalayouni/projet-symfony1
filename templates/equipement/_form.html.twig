
{% block body %}
{{ form_start(form) }}
<div  class="row g-3">
    {{ form_widget(form) }}
</div>
    <button class="btn btn-success">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

{% endblock %}
