{% extends 'base.html.twig' %}

{% block title %}Chercheur{% endblock %}

{% block body %}
    <h1>Chercheur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ chercheur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ chercheur.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ chercheur.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ chercheur.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ chercheur.password }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ chercheur.roles ? chercheur.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>
    <h2>Projets associés :</h2>
    <ul>
        {% for projet in projets %}
            <li>{{ projet.titre }} - {{ projet.description }}</li>
        {% endfor %}
    </ul>
    <a href="{{ path('app_chercheur_index') }}">back to list</a>

    <a href="{{ path('app_chercheur_edit', {'id': chercheur.id}) }}">edit</a>

    {{ include('chercheur/_delete_form.html.twig') }}
{% endblock %}
